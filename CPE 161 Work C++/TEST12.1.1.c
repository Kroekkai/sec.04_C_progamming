#include <stdio.h>

int main() 
{
    int numbers[3];
    int i;

    for (i = 0; i < 3; ++i) 
	{
        printf("??????????????????? %d: ", i + 1);
        scanf("%d", &numbers[i]);
    }

    for (i = 0; i < 3; ++i) 
	{
        printf("??? %d ???????????????????????????? %p\n", numbers[i], (void*)&numbers[i]);
    }

    return 0;
}
